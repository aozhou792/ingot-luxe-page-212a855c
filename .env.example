# Vercel Blob — order + receipt storage
BLOB_READ_WRITE_TOKEN=

# Resend — email notifications with receipt attachment
RESEND_API_KEY=
ORDER_NOTIFY_EMAIL=hxjspacex1@gmail.com
ORDER_FROM_EMAIL=Alibarbar Orders <onboarding@resend.dev>

# Admin page/API auth (choose a long random string)
ADMIN_API_KEY=

# Google Analytics 4 — client-side (Vite). Create a GA4 property and paste Measurement ID (G-XXXXXXXX).
# Also add this in Vercel → Project → Settings → Environment Variables for Production/Preview.
# After deploy: Admin → Custom definitions → register event params traffic_channel, ai_platform
# (and optionally session_source_custom, session_medium_custom). Mark purchase as a conversion.
# Custom channel group: Medium exactly matches "ai" → channel name "AI / LLM".
VITE_GA_MEASUREMENT_ID=
